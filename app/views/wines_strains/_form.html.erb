<%= form_with(model: wines_strain, local: true) do |form| %>
  <% if wines_strain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wines_strain.errors.count, "error") %> prohibited this wines_strain from being saved:</h2>

      <ul>
      <% wines_strain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :wine_id %>
    <%= form.text_field :wine_id %>
  </div>

  <div class="field">
    <%= form.label :strain_id %>
    <%= form.text_field :strain_id %>
  </div>

  <div class="field">
    <%= form.label :percentage %>
    <%= form.text_field :percentage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
